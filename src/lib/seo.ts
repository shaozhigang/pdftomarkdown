// seo.ts — retained for potential shared SEO utilities.
// Landing page metadata is now generated directly in each [locale]/*/page.tsx
// via generateMetadata() + getTranslations(), so the old landingMetadata()
// helper is no longer needed.
export {};
