# Public site URL — used for canonical links, sitemap, robots.txt and JSON-LD.
# Production (default in code): https://pdftomarkdown.run
# Local dev: uncomment the line below
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
